# This package contains the resilience patterns used throughout the application.
#
# These patterns are designed to make the pipeline more robust and fault-tolerant
# by gracefully handling common issues like transient network errors, API failures,
# and content filtering.
